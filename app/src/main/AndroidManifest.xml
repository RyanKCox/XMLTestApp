<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.revature.xmltestapp">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.XMLTestApp"
        tools:targetApi="31">
        <activity
            android:name=".IntentNav2"
            android:label="@string/intentNav2_name"
            android:parentActivityName=".IntentNav">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.revature.xmltestapp.IntentNav" />
        </activity>
        <activity
            android:name=".IntentNav"
            android:exported="false" />
        <activity
            android:name=".Layout"
            android:exported="false" />
        <activity
            android:name=".ScrollingTextView"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>